<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ShareDialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowMinWidthMinor">100%</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@style/ShareDialogAnim</item>
    </style>

    <style name="ShareDialogAnim">
        <item name="android:windowEnterAnimation">@anim/share_sliding_from_bottom_anim</item>
        <item name="android:windowExitAnimation">@anim/share_sliding_to_bottom_anim</item>
    </style>

</resources>
